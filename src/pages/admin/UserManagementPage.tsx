import { PagePlaceholder } from '../../components/ui/PagePlaceholder'

export function UserManagementPage() {
  return (
    <PagePlaceholder
      title="账号管理"
      description="管理员创建 / 停用员工账号（仅 admin 可见）"
    />
  )
}
