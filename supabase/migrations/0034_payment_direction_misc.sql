-- ============================================================================
-- 0034 — 付款方向新增「misc_expense(垫付杂项)」
-- 在 Supabase Dashboard → SQL Editor 整段粘贴运行。
--
-- 案件支出第三流：公司替客户垫付的杂项开销（体检/翻译/公证/申请费等）。
-- 与 to_company(付主代理) / to_referrer(付介绍人) 并列，在月度/财年账目的
-- 支出栏单独成组（2026-06-07 拍板）；不计入应收口径（应收只认 from_client）。
-- 纯增量（enum 加值），不改任何现有数据/默认值/RLS。可重复执行。
-- ============================================================================

alter type public.payment_direction add value if not exists 'misc_expense';
